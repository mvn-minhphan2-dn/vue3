name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: 
  push:
  pull_request:
jobs:
  eslint:
    name: runner / eslint
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write 
    steps:
      - name: Install NodeJS
        uses: actions/setup-node@v2
        with:
          node-version: '16'
      - name: Code Checkout
        uses: actions/checkout@v2

      - name: Install Dependencies  
        run:  yarn install
      - uses: reviewdog/action-eslint@v1
        with:
          # github_token: ${{ secrets.TOKEN }}
          reporter: github-pr-review
          eslint_flags: 'src/'

      - name: Code Linting
        run: yarn lint

# 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
